# Same freshness selection, different presentation order

The "what the user sees" rule is one thing: take the freshest N new ads. But notifications present newest-first, while previews present oldest-first (so the freshest lands last in the chat). These are the same selection with different presentation order — a single selector picks and sorts by freshness; the preview path reverses the output. Keeps the current user-visible behavior.
